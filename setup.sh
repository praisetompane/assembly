echo "Installing llvm"
    brew install llvm
echo "Done"

echo "Installing llvm gives us the assembler(as) and linker(ld)."
echo "We use these the same one would use gcc, clang, scala, python to convert high level code to machine code".
echo "They will convert the human readable machine code(i.e. assembly) into machine readable machine code(i.e. binary)."